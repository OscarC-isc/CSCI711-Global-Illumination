#include "globals.h"

RenderMode renderMode;

Camera camera;
Light light;
Object object[3];
Color background;

float worldWidth = 0.0f, worldHeight = 0.0f;
int screenWidth = 0, screenHeight = 0, numberOfLevels = 0;